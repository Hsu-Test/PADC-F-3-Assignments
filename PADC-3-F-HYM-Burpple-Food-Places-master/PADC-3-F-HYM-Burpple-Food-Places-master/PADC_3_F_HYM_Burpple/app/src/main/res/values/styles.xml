<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="white_text_black_border">

        <item name="android:textColor">@color/white_full</item>
        <item name="android:shadowDx">2</item>
        <item name="android:shadowDy">2</item>
        <item name="android:shadowRadius">2</item>
        <item name="android:shadowColor">@color/black_full</item>

    </style>
    <style name="green_text_green_border">



        <item name="android:textColor">@color/border_green</item>
        <item name="android:shadowDx">2</item>
        <item name="android:shadowDy">2</item>
        <item name="android:shadowRadius">2</item>
        <item name="android:shadowColor">@color/black_full</item>

    </style>
    android:textSize="@dimen/normal_text_size"

    <style name="register_form_style">

        <item name="android:layout_alignRight">@id/input_first_name</item>
        <item name="android:layout_alignLeft">@id/input_first_name</item>
        <item name="android:layout_marginTop">@dimen/margin_medium_2</item>
        <item name="android:textColorHint">@color/white_full</item>
        <item name="android:textSize">@dimen/normal_text_size</item>


    </style>

</resources>
